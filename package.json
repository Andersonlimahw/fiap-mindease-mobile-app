{
  "name": "bytebank",
  "version": "1.0.1",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest watch",
    "start": "EXPO_PUBLIC_USE_MOCK=false npx expo prebuild --clean",
    "start:firebase": "EXPO_PUBLIC_USE_MOCK=false npx expo start --clear --dev-client",
    "start:mocks": "EXPO_PUBLIC_USE_MOCK=true npx expo prebuild",
    "android": "EXPO_PUBLIC_USE_MOCK=false npx expo run:android",
    "android:clean": "EXPO_PUBLIC_USE_MOCK=false npm run watchman:del && npm run clean:android && npm run prebuild:clean && npx expo run:android",
    "ios": "EXPO_PUBLIC_USE_MOCK=false npx expo run:ios",
    "ios:mocks": "EXPO_PUBLIC_USE_MOCK=true npx expo run:ios",
    "ios:clean": "EXPO_PUBLIC_USE_MOCK=false npm run watchman:del && npm run clean:ios && npm run prebuild:clean && npx expo run:ios",
    "web": "EXPO_PUBLIC_USE_MOCK=true npx expo start --web",
    "typecheck": "npx tsc --noEmit",
    "build": "npx tsc -p .",
    "migrate:icons": "node scripts/migrate-icons.js",
    "build:android:dev": "npx eas build --platform android --profile development",
    "build:android:apk": "cd android && ./gradlew assembleRelease",
    "release:android:apk": "npm run build:android:apk && mkdir -p releases/android-versions/apk && cp android/app/build/outputs/apk/release/app-release.apk releases/android-versions/apk/bytebank-$(date +%Y%m%d-%H%M%S).apk && echo 'APK copiado para releases/android-versions/apk/'",
    "release:android:apk:versioned": "npm run build:android:apk && mkdir -p releases/android-versions/apk && cp android/app/build/outputs/apk/release/app-release.apk releases/android-versions/apk/bytebank-v$(node -p \"require('./package.json').version\")-$(date +%Y%m%d-%H%M%S).apk && echo 'APK versionado copiado para releases/android-versions/apk/'",
    "build:android:release": "cd android && ./gradlew bundleRelease",
    "build:ios:app:sim": "xcodebuild -workspace ios/bytebank.xcworkspace -scheme bytebank -sdk iphonesimulator -configuration Debug -derivedDataPath ios/build clean build",
    "build:ios:archive": "xcodebuild -workspace ios/bytebank.xcworkspace -scheme bytebank -configuration Release -destination 'generic/platform=iOS' -archivePath ios/build/bytebank.xcarchive clean archive -allowProvisioningUpdates",
    "build:ios:export": "xcodebuild -exportArchive -archivePath ios/build/bytebank.xcarchive -exportOptionsPlist ios/ExportOptions.plist -exportPath ios/build -allowProvisioningUpdates",
    "build:ios:release": "npm run build:ios:archive && npm run build:ios:export",
    "config:get": "npx expo config --json",
    "config:set": "npx dotenv -e .env -- npx expo config --type public",
    "expo:doctor": "npx expo-doctor",
    "expo:check": "npx expo install --check",
    "clean": "rm -rf .expo .expo-shared node_modules .turbo dist && npm install && npx expo install",
    "watchman:del": "watchman watch-del-all",
    "cache:metro": "rm -rf $TMPDIR/metro-* $TMPDIR/haste-* $TMPDIR/react-* $TMPDIR/react-native-*",
    "clean:deps": "rm -rf .expo .expo-shared node_modules && npm i",
    "clean:ios": "rm -rf ios/Pods ios/build && (cd ios && pod deintegrate && pod install) && rm -rf ~/Library/Developer/Xcode/DerivedData/*",
    "clean:android": "cd android && ./gradlew clean && cd .. && rm -rf android/app/build android/.gradle",
    "prebuild": "EXPO_PUBLIC_USE_MOCK=false npx expo prebuild --clean",
    "prebuild:clean": "npx expo prebuild --clean",
    "reset:all": "npm run watchman:del || true && npm run cache:metro && npm run clean:deps && npm run prebuild:clean && npm run clean:ios && npm run clean:android",
    "start:clean": "npm run reset:all && npx expo install && npx expo prebuild --clean -y && npx expo run ios"
  },
  "dependencies": {
    "@expo/vector-icons": "^15.0.2",
    "@react-native-async-storage/async-storage": "^2.2.0",
    "@react-native-firebase/app": "^23.3.1",
    "@react-native-firebase/auth": "^23.3.1",
    "@react-native-firebase/database": "^23.3.1",
    "@react-native-firebase/firestore": "^23.3.1",
    "@react-native-firebase/storage": "^23.3.1",
    "@react-native-google-signin/google-signin": "^16.0.0",
    "@react-native-picker/picker": "2.11.1",
    "@react-navigation/bottom-tabs": "^6.5.20",
    "@react-navigation/native": "^6.1.18",
    "@react-navigation/native-stack": "^6.9.27",
    "@shopify/flash-list": "2.0.2",
    "expo": "^54.0.0",
    "expo-apple-authentication": "~8.0.7",
    "expo-auth-session": "~7.0.8",
    "expo-build-properties": "~1.0.9",
    "expo-constants": "^18.0.9",
    "expo-document-picker": "~14.0.7",
    "expo-file-system": "^19.0.15",
    "expo-font": "^14.0.8",
    "expo-image": "^3.0.11",
    "expo-local-authentication": "^17.0.8",
    "expo-secure-store": "^15.0.8",
    "expo-web-browser": "~15.0.7",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native": "0.81.4",
    "react-native-get-random-values": "^1.11.0",
    "react-native-permissions": "^5.4.2",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0",
    "zod": "^4.3.5",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@react-native-community/cli": "latest",
    "@types/react": "~19.1.10",
    "@vitest/coverage-v8": "^2.1.9",
    "babel-preset-expo": "~54.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
